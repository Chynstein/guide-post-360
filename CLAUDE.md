- Changing the version numbers on my files to make sure everything updates as I continue to go test - use PowerShell commands to replace all occurrences at once (e.g., `powershell -Command "(Get-Content file) -replace '\\?v=43', '?v=44' | Set-Content file"`)
- Reading the full context of a situation to minimize bugs created while trying to implement new features or fix preexisting bugs
- When implementing changes to the code, make sure to use preexisting variable names and functions if possible to avoid creating duplicate unnecessary code
- When adding new properties to data objects (like textbox.description), check that the save/load pipeline (normalizeTextbox, saveMap, loadMap) preserves those properties
- Use dynamic properties from data objects (like room.mapFile) instead of hardcoded values or helper functions that return hardcoded values (like getMapFileForFloor())
- When matching/filtering data, use specific identifiers (mapFile) rather than generic ones (floor) that could match multiple unrelated items
- Trace the full data flow when fixing bugs - if data is created in one place and used in another, both places may need updates
- When adding early returns, check what code gets skipped and whether it needs to run before the return (may need to extract into helper functions)
- When adding special markers or syntax to textbox data (like ^ or ~), check ALL places where textbox text is rendered or processed - not just the immediate feature. This includes: canvas rendering (map_core.js), search indexing (map_pathfinder.js), and display formatting (showSuggestions)
- When replacing a variable or comparison pattern with a new one (e.g., switching from `floor` to `mapFile` for comparisons), search the ENTIRE codebase for ALL occurrences of the old pattern and update them consistently. Fixing only some instances while leaving others creates subtle bugs where the same logic works in some places but fails in others.
- When using CSS variables in new styles, verify they are defined in both `:root` (light mode) and `[data-theme="dark"]` (dark mode) sections before using them
- IMPORTANT TERMINOLOGY: "Dashboard" refers to login.html - the FIRST page users see when visiting the site, with Quick Tips, How-To Guide link, and Staff/Student buttons. There is NO post-login welcome page - after login, users go through terms agreement then directly to map_editor.html.
- Clarify ambiguous terminology with the user before implementing - don't assume what terms mean
- When changing UI flows that affect authentication (e.g., combining role buttons), update BOTH the frontend AND the backend auth logic to match
- Brand colors (like `--wb-blue`) used for text may be unreadable in dark mode - add explicit `[data-theme="dark"]` overrides for text colors
- `position: absolute` positions relative to the nearest positioned ancestor, not any arbitrary element - ensure the target container has `position: relative` and the element is a child of that container
- When code uses paired setup/teardown operations (save/restore, open/close, acquire/release, begin/end), always use try-finally to guarantee the cleanup runs - an uncaught exception can leave corrupted state that causes cascading failures elsewhere
- When filtering or culling objects based on coordinates, ensure the filter logic uses the same coordinate space as the rendering - if a transform (rotation, scale, translation) is applied to rendering, the filter bounds must account for that transform
- When multiple similar code blocks (like event handlers for mousedown/mouseup/mousemove/contextmenu) use a helper function or transformation, verify ALL of them use it consistently - if most use a helper like `screenToWorld()` but one doesn't, that's likely a bug from copy-paste or incremental changes, not an intentional difference
- When replacing the same hardcoded value in multiple places (e.g., replacing `#e5e7eb` with `var(--text-primary)` across a CSS file), use a single PowerShell command to replace all occurrences at once instead of making individual edits: `powershell -Command "(Get-Content file.css) -replace '#e5e7eb', 'var(--text-primary)' | Set-Content file.css"`
- For UI elements requiring permissions, prefer HIDING elements users can't use over showing DISABLED buttons. Use `{% if privilege in privileges %}...{% endif %}` to completely hide, not `{% if privilege not in privileges %}disabled{% endif %}` to show grayed out.
- NEVER use `2>nul` in bash commands - Git Bash doesn't recognize `nul` as the Windows null device and creates an actual file called "nul". Use `2>/dev/null` instead, or just omit stderr redirection entirely.
- Don't refer to me as "the user" - you're talking directly to me, so just say "you" or address me naturally.
- Flask-Minify can silently strip HTML from Jinja2 templates, causing elements to not appear in page source at all. When this happens, create DOM elements dynamically in JavaScript (document.createElement + document.body.appendChild) instead of fighting the template rendering.
- When many parameter combinations (like 16 direction/side/hinge combos for door swings) each need specific visual behavior, use explicit lookup tables mapping each combination to exact values rather than computing with formulas - formulas have subtle bugs that are hard to spot across many cases, while lookup tables are easy to verify and debug one entry at a time.
- If a third-party library doesn't support what you need (e.g., Flask-Limiter has no reset() method for clearing rate limits on successful login), implement simple custom logic rather than hacking around the library's limitations.